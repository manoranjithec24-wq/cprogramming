include <stdio.h>

int main() {
    int num=20000;
    if(num==450)
    {
        printf("the plance is avalibel");
    }
    else
    {
        printf("the plance is not avalibel");
    }

    return 0;
}